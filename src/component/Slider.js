import React from 'react'
import { useSelector } from 'react-redux'

const Slider = () => {
    const { listGame } = useSelector((state => state))
    return (
        <div className=''>

        </div >
    )
}

export default Slider